//Solution as follows

#include <bits/stdc++.h>
using namespace std;

int main() 
{
 int t;
 cin>>t;
 while(t--)
 {
   int X, Y, Z;
   cin>>X>>Y>>Z;    
   //Borrow the template from our sub-component
    if ((Z%X == 0) && (Z%Y == 0))
       {
        cout<<"ANY"<<endl;
       }
    else if (Z%X == 0)
       {
        cout<<"CHICKEN"<<endl;
       }
    else if (Z%Y == 0)
       {
        cout<<"DUCK"<<endl;
       }
    else
       {
        cout<<"NONE"<<endl;
        }

  }
return 0;
}
